import React from 'react';
import PageWrapper from '../util/PageWrapper';

const Home: React.FC = () => {
  return (
    <PageWrapper>
      <code>help</code>
    </PageWrapper>
  );
};

export default Home;
